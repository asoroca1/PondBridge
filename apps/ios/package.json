{
  "name": "@pondbridge/ios",
  "private": true,
  "version": "0.1.0",
  "description": "Native iOS and Android shells for PondBridge powered by the shared web build.",
  "scripts": {
    "build:web": "npm --workspace @pondbridge/web run build",
    "clean:dups": "node ../../scripts/cleanIcloudDuplicates.mjs apps/web/dist apps/ios/ios",
    "verify:bundle": "node ../../scripts/cleanIcloudDuplicates.mjs apps/ios/ios/App/App/public --check",
    "copy": "npm run build:web && npm run clean:dups && npx cap copy ios && npm run verify:bundle",
    "sync": "npm run build:web && npm run clean:dups && npx cap sync ios && npm run verify:bundle",
    "open": "npx cap open ios",
    "copy:android": "npm run build:web && npx cap copy android",
    "sync:android": "npm run build:web && npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug",
    "doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/network": "^8.0.1",
    "@capacitor/push-notifications": "^8.1.2",
    "@capacitor/status-bar": "^8.0.3"
  }
}
