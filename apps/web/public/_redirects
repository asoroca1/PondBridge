# Apple requires the association file to be served as JSON with no redirect.
# It must stay above the SPA fallback or it is answered with index.html and
# Universal Links silently stop verifying.
/.well-known/apple-app-site-association /.well-known/apple-app-site-association 200

# Cloudflare Pages: serve built assets directly.
# Missing files under these paths should stay 404.
/favicon.svg /favicon.svg 200
/manifest.json /manifest.json 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/assets/* /assets/:splat 200

# SPA fallback for application routes.
# Use "/" (not "/index.html") to avoid Cloudflare loop rejection.
/* / 200
